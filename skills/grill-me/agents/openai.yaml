interface:
  display_name: "Grill Me"
  short_description: "Sharpen a plan through focused questions"
  default_prompt: "Use $grill-me to pressure-test this plan before we act."
policy:
  allow_implicit_invocation: false
